# Garden Management

Small JS script for simple garden checks (watering, lights, and soil moisture increment).

This repo was created to contain `gardenManagment.js` so it can be safely committed without exposing other files from the home directory.
